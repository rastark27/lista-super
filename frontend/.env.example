# Solo necesario para el build de producción.
# En desarrollo local NO hace falta: el proxy de Vite reenvía /api al backend.
# En producción (Render): https://tu-backend.onrender.com/api
VITE_API_URL=
